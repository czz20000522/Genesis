# Kernel Issue Ledger

This file is the repo-owned ledger for active Genesis Kernel issues. Feishu Base is the collaboration inbox; this file is the durable project source for issues that still need code, verification, or user acceptance.

Retired issues must not remain here. Move accepted retirements to `docs/operations/kernel-retirement-log.md` as compact evidence: one sentence with the retirement conclusion plus the fixing commit evidence.

## Ledger Rules

- Keep only `new`, `open`, `in_progress`, or otherwise active issues in this file.
- Do not record application-specific feature work as kernel work unless it changes a kernel primitive.
- Do not add versioned HTTP route names as current contracts. HTTP is transport; current kernel endpoints are unversioned.
- `ready_for_acceptance` issues move to the retirement log as retirement candidates and leave this active ledger.
- Feishu/Base links may point to collaboration artifacts, but this repo must contain enough evidence to understand the current status without opening Feishu.
- Every active `KERNEL-*` issue must include a `Reference alignment` field that compares the issue to Codex, Reasonix, or an explicitly rejected drift risk.
- Reference alignment uses local reference checkouts only. Do not cite Genesis GitHub repositories, remote issues, releases, or pull requests as authority for this local kernel line unless the user explicitly asks for external publishing context.
- Before a non-trivial implementation slice starts, the related implementation plan or issue must include a Codex/Reasonix reference scan. The scan should identify inspected references, learned control-plane semantics, intentional differences, and remaining drift risks.
- Issues record the current gap between approved requirements/designs and the implementation. They must not carry raw requirements, design discussion, or the full production acceptance contract.
- Every active issue must cite an approved requirement and design unless it is an obvious bug or test gap. If an issue uses that exception, state the exception explicitly.
- Prefer `Gap`, `Next slice`, `Evidence`, and `Verification` over broad `Problem` or `Suggestion` text when adding new issues.
- Do not use issues as debug logs. Routine info, stream chunks, repeated status polling, and exploratory notes stay out of this ledger unless they identify a current implementation gap.
- When an issue removes a concept from the current kernel contract, long-term tests must assert the positive replacement behavior. Do not keep permanent tests whose only purpose is locking retired names, aliases, routes, or historical helper APIs out of the tree; use temporary scans or retirement-log evidence for cleanup windows, then fold the guard into the current owner contract.
- Development artifacts and historical local data are not compatibility obligations. Do not create or keep issues whose only purpose is migrating, cleaning, importing, or preserving old local generated state unless that state is part of the approved current kernel contract.
- Every implementation slice must finish with a drift check against the governing requirement, design, implementation plan, issue, and BDD feature. In-scope drift is fixed before commit. Out-of-scope drift is recorded here as an active issue with evidence and next slice before commit.
- Periodic governance review checks architecture, feature behavior, directory structure, and document lifetime together. Completed plans and stale documents should be deleted or condensed instead of spawning issues that only preserve old notes.

## Active Issues

### KERNEL-TOOL-LOOP-PAUSE-RESUME-20260625 - P2 - Tool-loop round guard should pause resumably instead of failing production work

- Status: open.
- Area: Model Gateway / Interface Kernel / Work Recovery.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`; `docs/requirements/kernel-shell-and-job-control.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`; `docs/design/kernel-shell-and-job-control.md`.
- 标题: Tool-loop round guard should pause resumably instead of failing production work.
- 问题: Genesis currently hard-limits model tool rounds with `maxModelToolRounds = 4`. When the provider still requests tools at that boundary, the kernel writes `turn.failed` with `tool_loop_limit_exceeded` and returns an error. This prevents infinite loops, but it also converts legitimate long-running exploration into a terminal failure instead of a resumable checkpoint. For an agent kernel, the guard should protect the system while preserving already committed tool evidence and giving the user/application a controlled continuation path.
- 建议: Define a bounded tool-loop pause/resume contract. When the configured tool-round budget is reached, the kernel should stop before executing the next tool batch, write a typed paused/interrupted/continuation-required fact with preserved provider-visible tool rounds, and expose a `continue` path through the normal `turn.submit`/session interface. Repeated continuations should remain budgeted and idempotent. The model should see prior committed tool calls/results on continuation, but hidden control ids and raw audit fields must stay out of provider context.
- 证据: `internal/kernel/model_tools.go` defines `const maxModelToolRounds = 4`. `internal/kernel/kernel.go` loops until that bound and, at the final round, appends `TurnError{Code: "tool_loop_limit_exceeded"}` before returning `model tool loop exceeded the maximum number of rounds`. Existing Genesis tests cover provider-visible tool rounds and idempotent retry, but the repository scan found no active issue or focused test for max tool-round continuation semantics. Local Reasonix treats positive `maxSteps` as a resumable guard: `internal/agent/agent.go::Run` returns a “paused after N tool-call rounds” message after preserving session state so the user can continue, and its coordinator tests assert the pause message. Codex similarly treats turns, interruptions, and resumes as first-class control surfaces rather than conflating all control stops with provider/tool failure.
- 验证: Add tests where a provider repeatedly emits valid tool calls beyond the configured round budget. Verify already committed tool results remain in ledger/provider projection, the boundary event is not `turn.failed` unless a true infrastructure error occurred, continuation can resume with the prior tool rounds visible to the provider, no duplicate side effects occur on retry, and hidden kernel ids remain absent from provider context. Include a repeated-continuation or malicious-loop case proving the guard remains bounded. Run focused tool-loop tests plus `go test ./internal/kernel -run "Test.*Tool.*Loop|Test.*ProviderContext.*ToolRound|TestHTTP.*Idempotency" -count=1`.
- 优先级: P2.
- Gap: The current round cap is a safety backstop, but its failure semantics are too coarse for production agent work. It lacks a durable paused/continuation state, an idempotent resume contract, and tests proving round-budget stops preserve committed facts without re-executing effects.
- Next slice: Write the requirement/design slice first: define the exact turn status, event type, retry/continue API shape, and provider-context projection rules. Then implement the smallest behavior test around a fake provider and a harmless registered tool before extending it to shell/job scenarios.
- Reference alignment: Aligned with Reasonix's `maxSteps` resumable pause semantics and Codex's control-plane separation for interruption/resume. Genesis should intentionally differ by expressing the pause as a kernel event/projection contract rather than a plain agent error string.

### KERNEL-TOOL-LOOP-STORM-GUARD-20260625 - P2 - Repeated tool failure and repeated write success need a model-visible loop guard

- Status: open.
- Area: Tool Runtime / Model Gateway / Safety Backstop.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`; `docs/requirements/kernel-shell-and-job-control.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`; `docs/design/kernel-shell-and-job-control.md`.
- 标题: Repeated tool failure and repeated write success need a model-visible loop guard.
- 问题: Genesis has strong idempotency for explicit transport/tool retry keys, but it does not appear to detect model-level tool storms inside one user turn: the model can repeatedly emit semantically identical failing tool calls, or repeatedly emit the same successful write-like command with fresh kernel-generated tool-call event ids. Because each provider call receives a new kernel slot, ordinary idempotency does not stop a loop that burns the tool-round budget or repeats side effects.
- 建议: Add a kernel-owned storm-guard contract that observes consecutive tool outcomes within one turn. Repeated identical failure signatures should return a model-visible directive to change approach while preserving the original error; repeated successful write-like signatures should be blocked before another effect and returned as a non-executed guarded tool result. The guard must reset on real progress, partial success, a different batch shape, or a new turn. Shell-specific matching should start conservative: only clearly write-like commands or explicit non-shell write tools should enter repeat-success detection.
- 证据: `internal/kernel/model_tools.go` binds each model tool call to a fresh kernel `tool_call_event_id`; `shellInvokeModelToolResult` uses that idempotency key for the concrete shell operation. This protects replay of the same admitted operation, but not a model that reissues the same write command in later tool rounds with a fresh event id. Repository scans show tests for turn/shell/work idempotency and tool-loop ordering, but no test for repeated same-error failures or repeated same-success write calls inside a single turn. Local Reasonix has explicit storm guards in `internal/agent/storm_test.go` and `internal/agent/agent.go`: repeated same-error batches produce a `[loop guard]` model-visible result, and repeated identical write-like successes are blocked before another write.
- 验证: Add fake-provider tests that force three consecutive malformed/failing calls with different argument bytes but the same tool/error signature, and verify the later model-visible tool result contains a structured loop-guard directive rather than only the raw error. Add a second test where repeated same write-like shell intent would create duplicate files or append duplicate content; verify the repeat is blocked before a new effect and the user/session projection records guarded evidence. Add reset tests proving a successful different tool, partial success, or a new turn clears the guard. Run focused tool-loop/idempotency tests and then `go test ./internal/kernel -run "Test.*Tool.*Storm|Test.*Idempotency|Test.*Tool.*Loop" -count=1`.
- 优先级: P2.
- Gap: Current idempotency is retry-oriented, not behavior-oriented. It prevents duplicate effects for the same explicit key, but it does not catch a stuck model that generates fresh keys for the same failing or write-like action.
- Next slice: Specify the signature rules before implementation. For Phase A, guard repeated same-error tool batches and a narrow allowlist of write-like shell/write primitives; do not attempt broad semantic command equivalence for arbitrary shell text.
- Reference alignment: Aligned with Reasonix's storm breaker and repeat-success guard. Genesis should intentionally keep the guard inside Tool Runtime/Model Gateway outcome handling, not inside provider adapters or UI shells. Codex-style idempotent replay remains necessary but insufficient for model-level loop storms because provider-generated calls can be fresh while semantically repeating the same effect.

### KERNEL-SKILL-CATALOG-SCAN-BOUNDS-20260625 - P2 - Skill catalog scanning needs bounded depth, count, and file-size guards

- Status: open.
- Area: Model Gateway / Skill Catalog / Context Projection.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`.
- 标题: Skill catalog scanning needs bounded depth, count, and file-size guards.
- 问题: Genesis correctly keeps skill bodies out of the default provider context and rejects unsafe metadata, duplicate names, and linked paths, but the configured skill-root scanner is still an unbounded filesystem walk that reads every discovered `SKILL.md` body in full before parsing metadata. A misconfigured root, a very deep directory tree, or a huge `SKILL.md` can make turn startup/capability inspection slow or memory-heavy even though only name/description metadata is needed.
- 建议: Add a kernel-owned skill catalog scanning contract with explicit maximum recursion depth, maximum discovered skill candidates per root, maximum `SKILL.md` metadata read size, and stable exclusion reasons such as `scan_depth_exceeded`, `scan_count_exceeded`, and `skill_file_too_large`. The scanner should read only a bounded prefix sufficient for front matter, skip generated/vendor-style heavy directories if such policy is adopted, and keep model-visible skill index behavior unchanged.
- 证据: `internal/kernel/skill_catalog.go::loadSkillCatalogWithDiagnostics` calls `filepath.WalkDir` over each configured root without a depth or candidate-count cap, and calls `os.ReadFile(path)` for each `SKILL.md` before `parseSkillMetadata`. Existing skill tests cover metadata-only provider context, missing/malformed roots, unsafe metadata, duplicate names, symlink/link rejection, and budgeted skill-index text, but no test covers oversized skill files, excessive candidate counts, or deep-root traversal bounds.
- 验证: Add tests that create a deep nested tree beyond the configured scan depth, more than the allowed number of candidate skill files, and a `SKILL.md` larger than the metadata read cap. Verify excluded entries do not enter provider context or `/capabilities`, exclusion reasons are path-free and bounded, and normal safe skills still load. Run `go test ./internal/kernel -run "Test.*Skill" -count=1`.
- 优先级: P2.
- Gap: Skill metadata is a provider-context input owned by the kernel. The current implementation bounds the final skill-index text but not the filesystem scan and file read that produce that index.
- Next slice: Introduce bounded scan constants and path-free exclusion reasons, add focused regression tests for deep roots, too many candidate skills, and oversized `SKILL.md`, then verify existing metadata-only and unsafe-metadata tests continue to pass.
- Evidence: Local Reasonix bounds skill-root scanning in `internal/installsource/skill.go::scanSkillRoot` with maximum recursion depth and skill count, and caps the prompt skill index in `internal/skill/index.go`. Codex also has dedicated skill tests and context snapshot normalization around `SKILL.md` surfaces. Genesis currently has no equivalent test for scan depth/count/file size.
- Verification: `go test ./internal/kernel -run "Test.*Skill" -count=1`; broader verification should include `go test ./internal/kernel -count=1` after implementation because skill catalog projection affects provider context, `/capabilities`, and context inspection behavior.
- Reference alignment: Aligned with Reasonix's bounded skill root scan and bounded skill index. Genesis intentionally differs by not exposing full skill paths or bodies in model context, but it should still bound the filesystem scan that produces the safe metadata index.

### KERNEL-PROVIDER-REASONING-REPLAY-GUARD-20260625 - P2 - Provider reasoning output needs an explicit non-replay regression guard

- Status: open.
- Area: Model Gateway / Provider Context Projection / Test Governance.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`.
- 标题: Provider reasoning output needs an explicit non-replay regression guard.
- 问题: Provider-native hidden reasoning such as DeepSeek `reasoning_content` is response-only data. Genesis currently appears to ignore it implicitly, but there is no executable guard proving it cannot enter durable transcript, provider context replay, provider command request JSON, OpenAI-compatible request messages, UI timeline, or audit replay.
- 建议: Add reference-inspired regression tests before future reasoning UI/streaming work expands the protocol. The tests should feed `reasoning_content` through the active OpenAI-compatible/provider-context path and prove only visible assistant content survives into model-visible replay.
- 证据: Reasonix has an explicit `TestBuildRequestDropsReasoningContent` guard; Genesis has provider/context projection tests but no test mentioning `reasoning_content`, `ReasoningContent`, or hidden reasoning non-replay.
- 验证: The future focused tests must fail if hidden reasoning text appears in `ProviderContextProjection`, provider command JSON, OpenAI-compatible request messages, session timeline, or audit replay.
- 优先级: P2.
- Gap: Genesis docs state that session transcript may store only product-approved reasoning summaries or notices and must not store provider raw payloads or hidden reasoning chains. The current OpenAI-compatible adapter ignores unknown provider response fields such as DeepSeek-style `reasoning_content`, but there is no explicit reference-inspired regression test proving response-only reasoning fields cannot enter `ModelResponse`, durable transcript, conversation history, provider command request, or a later OpenAI-compatible request. This is currently a test and guard gap, not evidence of an observed runtime leak.
- Next slice: Add focused tests around the active provider/context surfaces: an OpenAI-compatible response containing `message.reasoning_content` plus visible `content` must produce only the visible final text and normalized usage; a later same-session provider request must not contain `reasoning_content` or the hidden reasoning text; provider/context/audit/session projections must continue to omit hidden reasoning unless a future product-approved reasoning-summary event type is explicitly introduced with separate projection rules.
- Evidence: Local Reasonix has `internal/provider/openai/openai_test.go::TestBuildRequestDropsReasoningContent`, which proves assistant `ReasoningContent` is response-only and must not be echoed back to DeepSeek/OpenAI requests. It also documents that DeepSeek may reject or charge for reasoning round-trip. Genesis currently has `TestOpenAICompatibleProviderCompletesAgainstCompatibleServer`, `TestOpenAICompatibleProviderNormalizesPromptCacheUsage`, `TestObservabilityProjectionsSeparateRawAuditAndProviderContext`, and `TestSubmitTurnProviderContextIncludesSameSessionHistory`, but no test mentioning `reasoning_content`, `ReasoningContent`, or a hidden reasoning non-replay case. `docs/field-reference.md` notes reasoning-token accounting is inspection-only and `docs/requirements/kernel-foundation-capabilities.md` says hidden reasoning chains do not enter transcript.
- Verification: Focused verification should include `go test ./internal/kernel -run "TestOpenAICompatible.*Reasoning|Test.*ProviderContext.*Reasoning|TestObservabilityProjectionsSeparateRawAuditAndProviderContext" -count=1`, followed by `go test ./internal/kernel -count=1` and `go test ./... -count=1`. The tests should fail if hidden reasoning text appears in `ProviderContextProjection`, provider command JSON, OpenAI-compatible request messages, session timeline, or audit replay.
- Reference alignment: Aligned with Reasonix's explicit DeepSeek/OpenAI `reasoning_content` non-replay guard and Codex's broader separation between model-visible context, UI/reasoning summaries, and raw protocol/event inspection. The active drift risk is future UI/streaming/reasoning-summary work accidentally treating provider-native hidden reasoning as conversation history or provider-visible context.

### KERNEL-PROVIDER-VISIBLE-FINAL-REPAIR-20260625 - P2 - Reasoning-only or empty final provider output should have a bounded visible-answer repair path

- Status: open.
- Area: Model Gateway / Provider Loop / Live LLM Usability.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`.
- 标题: Reasoning-only or empty final provider output should have a bounded visible-answer repair path.
- 问题: The current OpenAI-compatible provider returns `provider returned no assistant content` when the provider response has no visible assistant `content`. That is safe, but it turns a model-visible repairable condition into a terminal provider failure. Reasoning-capable OpenAI-compatible providers can emit reasoning without visible final text; a user-facing turn should get a bounded repair step asking for a visible answer before the kernel fails the turn.
- 建议: Add a Model Gateway repair path and tests for empty/reasoning-only final responses. The first empty final should produce a model-visible repair request that asks for a visible final answer without replaying hidden reasoning. Repeated empty finals should stop after a small bounded count and record a structured turn failure.
- 证据: Reasonix `internal/agent/empty_final_test.go::TestRunRetriesReasoningOnlyFinalAnswer` retries after a reasoning-only answer and verifies the final assistant content becomes visible. `TestRunStopsAfterRepeatedEmptyFinalAnswers` proves the repair loop is bounded. Genesis has `openai_compatible.go` returning an error for empty assistant content and tests for provider failures/idempotent replay, but no visible-final repair test.
- 验证: Add focused tests where an OpenAI-compatible/provider fake first returns empty visible content and then returns visible text; the kernel should complete with the visible text and record repair evidence without exposing hidden reasoning. A repeated-empty case should fail with a structured reason after the bound, without tool execution, external effects, or unbounded provider calls.
- 优先级: P2.
- Gap: Genesis currently distinguishes provider failures from tool failures and records failed turns durably, but it lacks a bounded self-repair loop for provider responses that are syntactically valid yet lack visible assistant output. This is especially relevant before live LLM usage grows, because a reasoning-only result is not the same class as network/provider unavailability.
- Next slice: Specify the repair as Model Gateway behavior, not provider-adapter magic. The adapter should surface a typed empty-visible-output response or error class; the kernel loop should decide whether to issue a bounded visible-answer repair prompt, then record model context accounting and failure evidence without hiding repeated empty outputs.
- Evidence: `internal/kernel/openai_compatible.go` currently returns `errors.New("provider returned no assistant content")` for empty content. Current Genesis tests cover provider unavailable replay and live provider non-empty final smoke, but no test covers reasoning-only or empty-final repair. Reasonix has explicit reference tests for both retry and bounded stop.
- Verification: Future verification should include a focused `go test ./internal/kernel -run "Test.*VisibleFinal|Test.*EmptyFinal|Test.*ReasoningOnly" -count=1`, then `go test ./internal/kernel -count=1` and `go test ./... -count=1`.
- Reference alignment: Aligned with Reasonix's visible-answer repair loop for reasoning-only finals. The intentional Genesis difference should be that hidden reasoning remains non-replayed per `KERNEL-PROVIDER-REASONING-REPLAY-GUARD-20260625`; only the visible-answer repair instruction enters provider context.

### KERNEL-PROVIDER-RETRY-DEGRADATION-CONTRACT-20260625 - P2 - Provider transient failures need a bounded retry/degradation contract before live LLM reliance

- Status: open.
- Area: Model Gateway / Provider Loop / Reliability / Test Governance.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`.
- 标题: Provider transient failures need a bounded retry/degradation contract before live LLM reliance.
- 问题: Current provider adapters execute a single provider attempt, and the kernel turns any provider error into a durable `turn.failed`. That is fail-closed, but it treats retryable transport failures, 408/429/5xx, auth/quota/config failures, provider-command process failures, and future stream interruption as the same operational class. A production Model Gateway needs one kernel-owned retry/degradation contract; otherwise shells, adapters, or application code will be tempted to add private retry loops that can duplicate effects or hide the real failure class.
- 建议: Add Model Gateway owned provider failure classification and bounded retry/degradation behavior. For the current non-streaming provider surface, retry only classified pre-output transient failures such as temporary network errors, 408, 429, and 5xx with bounded backoff and `Retry-After` handling; fail fast for 400, 401, 402, 403, 422, configuration, credential, and adapter-shape failures. Record attempt/retry/degradation evidence in inspection/audit projections without leaking credentials or raw provider payloads. Provider retries must not re-execute already admitted tools, must not mark kernel observations delivered before a successful provider response, and must preserve idempotent replay semantics for failed turns.
- 证据: Reasonix `internal/provider/retry.go` classifies retryable status codes, honors `Retry-After`, fails fast on auth/client errors, and documents that retries cover only the connection/header phase before stream body output. Reasonix `internal/provider/openai/reconnect_test.go` proves early stream reset before model output may replay, while reset after partial output must surface as interruption and must not replay. Codex `codex-rs/core/src/responses_retry.rs`, `codex-rs/core/tests/suite/stream_no_completed.rs`, and `codex-rs/core/tests/suite/websocket_fallback.rs` show bounded stream retry, user-visible reconnect/fallback evidence, and transport fallback after retry budget exhaustion. Genesis `internal/kernel/openai_compatible.go` currently performs one HTTP request and returns `provider returned status N` for all non-2xx responses; `internal/kernel/provider_command.go` runs the external provider command once; `internal/kernel/kernel.go` appends `turn.failed` on provider error without classification or retry policy.
- 验证: Add focused tests proving a retryable OpenAI-compatible failure such as 500 or 429 can recover within a bounded attempt budget and records attempt evidence; 401/402/403/422/config failures fail fast with one attempt and a typed reason; provider-command start/config/schema failures do not retry as ordinary model work; provider retry after a tool result has been committed does not re-execute the tool and does not mark kernel observation delivery until the next provider call succeeds; idempotent retry of a failed turn still returns the original failed turn evidence without a new provider call. When streaming provider support is introduced, add the no-replay-after-visible-output guard before enabling stream reconnect.
- 优先级: P2.
- Gap: Genesis has durable provider failure and idempotent failed-turn replay, but it lacks a reference-inspired provider retry/degradation contract. This is not a request to add application-level retries or model-vendor-specific fallback logic; it is a kernel Model Gateway reliability boundary that must prevent duplicated effects while improving live LLM resilience.
- Next slice: Specify a provider error taxonomy and retry attempt evidence in the Model Gateway requirement/design, then implement the smallest non-streaming retry planner around OpenAI-compatible/provider_command surfaces. Defer streaming reconnect until Genesis has a streaming provider protocol, but record the no-replay-after-output redline now so future streaming work cannot silently replay partial assistant output.
- Reference alignment: Aligned with Reasonix's provider retry classification and output-after-stream no-replay guard, and with Codex's bounded Responses stream retry/fallback tests. Genesis can intentionally stay smaller in P0, but it should not leave provider failure as one unclassified terminal bucket once real LLM usage becomes part of the production path.

### KERNEL-CONTEXT-COMPACTION-STUCK-GUARD-20260625 - P2 - Auto compaction needs a stuck/cache-thrash guard for too-small windows

- Status: open.
- Area: Model Gateway / Context Compaction / Provider Context Projection / Test Governance.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`.
- 标题: Auto compaction needs a stuck/cache-thrash guard for too-small windows.
- 问题: Genesis currently has successful compaction, summarizer failure recording, failure backoff, provider-backed recent-tail accounting, and cache-stability evidence. It does not yet have an executable guard for the production case where compaction succeeds but does not reclaim enough effective context, causing every subsequent turn to compact again and repeatedly break prompt-cache locality. Failure backoff does not cover this case because the summarizer returns success.
- 建议: Add a compaction stuck guard owned by the kernel compaction runner. The first slice should be a requirement/design and behavior-test update: when a too-small context window or huge retained recent tail causes repeated consecutive successful compactions without effective headroom/cache recovery, the runner should pause/defer auto-compaction with typed evidence and a user/inspection notice. Manual compaction can remain available, but auto compaction should not thrash every turn.
- 证据: Genesis `internal/kernel/context_compaction.go` triggers auto compaction whenever provider-reported `InputTokens` crosses the limit, and `contextCompactionBackoff` only defers after `context.compaction.failed`. `TestAutoCompactionBacksOffAfterSummarizerFailure` covers failure retry/backoff, while `TestAutoCompactionRecordsUsageEconomicsAndCacheStability` records cache facts but does not assert that cache collapse or consecutive successful compactions pause future auto compaction. Local Reasonix has `internal/agent/compact_loop_e2e_test.go::TestCompactionPausesWhenWindowTooSmall` and `TestCompactionHealthyWindowNeverLoops`, plus `internal/agent/cachehit_e2e_test.go::TestCacheHitSurvivesTooSmallWindow`, proving the reference behavior: cap repeated compactions, emit an auto-compaction-paused notice, and preserve cache stability in misconfigured windows.
- 验证: Add focused tests that drive a long tool-heavy session with a context window too small for the retained tail. The tests should fail if auto compaction runs on many consecutive turns or keeps collapsing cache after successful compactions; they should pass only when the runner emits a `context.compaction.deferred` or equivalent paused evidence with a clear reason and leaves later provider context append-only until enough new evidence justifies retry. Companion tests should prove a healthy window still compacts when needed and does not pause.
- 优先级: P2.
- Gap: This is a production reliability and cost-control guard gap for the existing auto-compaction feature. It should not be solved in WebUI, provider adapters, or external shells; the kernel compaction runner owns the decision because it owns provider usage accounting, compaction state, and provider context projection.
- Next slice: Define the stuck criteria before implementing executor behavior. Candidate signals include consecutive successful auto compactions, compacted-region size versus retained-tail size, repeated compaction within the retry window, and cache-stability trend. Keep the first implementation conservative: prefer pausing auto compaction with typed evidence over trying to locally estimate token savings.
- Reference alignment: Aligned with Reasonix's stuck guard for too-small windows and cache-hit preservation tests. Codex also treats compaction as a lifecycle with analytics/status and replacement-history semantics rather than an unbounded loop. Genesis can keep its simpler provider-backed accounting model, but it should not intentionally drift into repeated successful compactions that provide no effective recovery.

### KERNEL-JOB-PROGRESS-IDLE-CONTINUATION-20260623 - P2 - Local managed job streaming and attach capability

- Status: open.
- Area: Tool Runtime / Interface Kernel / Model Gateway.
- Requirement: `docs/requirements/kernel-shell-and-job-control.md`.
- Design: `docs/design/kernel-shell-and-job-control.md`.
- Gap: Kernel now has the first `job.output` snapshot contract, the local managed shell executor emits bounded sparse live output snapshots, and foreground shell interruption is capability-gated. The local executor explicitly does not advertise foreground attach support, so interrupted foreground shell work is killed with `interrupt_reason=foreground_attach_unavailable_killed` and no managed job is forged. Requirement/design now define the attach-capable executor contract and no-forged-job redline. Remaining gap is true foreground shell attach/detach implementation through a future attach-capable executor.
- Next slice: Implement or integrate an attach-capable executor without exposing process ids, host signals, or process handles to model-visible tools or transport callers.
- Evidence: `6e3287525` adds `InterruptSession`, `POST /sessions/{id}/interrupt`, `assistant.interrupted`, `operation.interrupted`, and tests proving provider-step interruption does not cancel an existing background job. `docs/requirements/kernel-shell-and-job-control.md` and `docs/design/kernel-shell-and-job-control.md` now define foreground attach as an executor capability, require kernel-bound managed-job identity, and forbid exposing host process ids, signals, or handles to model/HTTP callers. `TestSubmitTurnDeliversAllTerminalJobObservationKinds` proves user-triggered continuation drains queued terminal observations without autonomous wakeup. `TestJobOutputSnapshotIsDurableButNotProviderObservation`, `TestManagedJobExecutorCanReportOutputSnapshot`, `TestManagedJobExecutorOutputSnapshotIsBounded`, `TestManagedJobExecutorCannotRedirectOutputSnapshotIdentity`, and `TestUITimelineFoldsDirectManagedJobEventsByJobID` prove `job.output` snapshots are bounded durable session/UI facts while remaining out of default provider observation delivery, kernel-bound to the originating job, and folded for direct shell transports. `TestLocalManagedJobExecutorEmitsSparseOutputSnapshot`, `TestManagedJobOutputCaptureDoesNotEmitEveryChunk`, `TestManagedJobOutputCaptureCapsDurableSnapshotsPerJob`, and `TestManagedJobOutputCaptureStopsAfterTruncatedSnapshot` prove the local executor reduces live stdout/stderr to sparse durable snapshots instead of persisting every transport chunk or allowing unbounded per-job progress persistence. `TestInterruptSessionDuringForegroundShellWritesInterruptedToolResult`, `TestLocalManagedJobExecutorDoesNotAdvertiseForegroundAttach`, and `TestForegroundInterruptReasonStaysKillFallbackUntilAttachIsImplemented` prove the current foreground interrupt path records the truthful kill fallback and does not forge managed-job attach facts.
- Verification: Remaining verification must prove any future attach-capable executor can convert interrupted foreground shell work into kernel-owned managed-job facts while keeping host process handles hidden behind executor semantics.
- Reference alignment: Aligned with Codex background terminal list/terminate control surfaces and Reasonix's `ProgressFunc` plus session-scoped job manager. The active drift risk is turning live progress into provider-owned context, UI-owned truth, or a strong audit log instead of a kernel-owned durable fact with separate projections.

### KERNEL-FOREGROUND-PROCESS-TREE-CLEANUP-GUARD-20260625 - P2 - Foreground shell timeout and interruption need process-tree escape regression tests

- Status: open.
- Area: Tool Runtime / Shell Executor / Job Control / Test Governance.
- Requirement: `docs/requirements/kernel-shell-and-job-control.md`.
- Design: `docs/design/kernel-shell-and-job-control.md`.
- 标题: Foreground shell timeout and interruption need process-tree escape regression tests.
- 问题: Genesis foreground shell execution now calls platform-specific process-tree termination hooks, but current acceptance tests only prove the kernel records timeout/interrupted operation facts for direct long-running commands. They do not prove a shell command that spawns child, grandchild, or detached descendant work cannot continue writing files or holding pipes after the kernel has recorded terminal timeout/interruption evidence. If that hook drifts or is incomplete on a platform, the ledger can claim a command was stopped while real side effects continue outside the kernel's recorded lifecycle.
- 建议: Add reference-inspired regression tests around the foreground executor contract. The tests should run foreground timeout and session interruption against commands that spawn descendant work which would write a marker after parent cancellation; the marker must not appear after the kernel records `operation.failed` with `foreground_timeout` or `operation.interrupted` with `foreground_attach_unavailable_killed`. On Windows, also decide whether `taskkill /T /F` is the accepted production boundary or whether a Job Object based executor is required for detached descendants; if the current local executor cannot guarantee detached-descendant cleanup, document that limitation and keep production stronger executor/sandbox work blocked until the stronger executor exists.
- 证据: Genesis `internal/kernel/process_runtime.go` uses `exec.CommandContext` and calls `configureShellProcessTermination`; `process_termination_windows.go` uses `taskkill /F /T` plus `Process.Kill`, and `process_termination_other.go` uses `Setpgid` plus negative-PID `SIGKILL`. Current focused tests such as `TestSubmitTurnForegroundShellTimeoutRecordsTerminalOutcome` and `TestInterruptSessionDuringForegroundShellWritesInterruptedToolResult` verify structured timeout/interruption evidence, but no test mentions process-tree, child process, grandchild, detached descendant, or post-cancel marker effects. Codex `codex-rs/core/tests/suite/cli_stream.rs` wraps subprocess tests in a cleanup guard that kills the whole process group or Windows process tree. Reasonix `internal/tool/builtin/bash_kill_*` treats kill-tree as bash executor responsibility, and `internal/proc/kill_windows_test.go` explicitly tests a surviving grandchild holding a pipe plus a Job Object tracked tree case.
- 验证: Add focused tests that fail if a child/grandchild spawned by foreground shell timeout or user interruption survives long enough to write an after-cancel marker or keep `cmd.Wait` blocked beyond the bounded cleanup window. Keep the tests platform-aware and skip only when the platform lacks the required shell primitive. Re-run `go test ./internal/kernel -run "Test.*Foreground.*ProcessTree|Test.*Foreground.*Timeout|Test.*InterruptSessionDuringForegroundShell" -count=1`, then `go test ./internal/kernel -count=1` before retirement.
- 优先级: P2.
- Gap: This is a current-stage test and executor-boundary gap, not proof that the existing process termination hook is broken. The risk is that the approved shell/job-control contract depends on process-tree cleanup, while the executable evidence currently covers only direct process timeout/interruption and terminal event projection.
- Next slice: Introduce platform-aware process-tree escape fixtures under the shell/job-control test surface. If the fixtures expose an implementation gap, fix it inside the executor boundary rather than changing model-visible tool semantics or adding process ids/signals to the public contract.
- Reference alignment: Aligned with Codex's process-group cleanup guard for CLI subprocess tests and Reasonix's explicit kill-tree/Job Object tests. Genesis should intentionally keep host PIDs, signals, `taskkill`, process groups, and Job Objects hidden behind executor semantics, but the hidden executor contract still needs regression evidence.

### KERNEL-TOOL-PARALLEL-FAILURE-SEMANTICS-20260625 - P2 - Parallel pure-read batches need explicit per-call versus fatal failure semantics

- Status: open.
- Area: Tool Runtime / Tool Scheduling / Model-Visible Results / Recovery.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`.
- 标题: Parallel pure-read batches need explicit per-call versus fatal failure semantics.
- 问题: Genesis now executes eligible `pure_read` batches concurrently and commits results in provider order, but the failure contract for a concurrent batch is not yet explicit. `executeToolBatchConcurrently` cancels the batch and returns an error on the first tool execution error, after which `handleToolExecutionError` records one turn-level failure and no sibling `tool.result` events are committed. Current `resource_read` turns ordinary malformed or unknown-resource cases into preflight repair results, so this gap is mainly about infrastructure failures and future non-shell `pure_read` tools. Without a written and tested boundary, future pure-read tools may accidentally turn a per-call read failure into a whole-turn failure, or conversely hide a fatal runtime failure as an ordinary tool result.
- 建议: Define a parallel-batch failure taxonomy before adding more parallel read tools. Non-fatal, model-actionable per-call failures should produce a `tool.result` for that call and keep sibling results ordered; fatal executor/runtime failures may fail the turn, but the contract must state whether already completed sibling pure-read observations are committed, discarded, or replayed. Add regression tests for at least: one pure-read succeeds while another returns a per-call failure; one pure-read hits `ErrToolInfrastructureFailed`; a runner returns duplicate/missing/unexpected results; and turn interruption cancels the batch without forging successful tool results. Keep `workspace_write`, `process_start`, `process_io`, `kernel_state_write`, and `external_side_effect` out of the concurrent runner until their idempotency/replay contracts are separately proven.
- 证据: Genesis `internal/kernel/tool_execution.go` runs concurrent pure-read batches through `executeToolBatchConcurrently`; any goroutine error cancels the context and returns a batch error, and `executeToolBatchesWithRunner` records a turn failure before committing any result from that batch. Existing tests prove planner fences, true pure-read concurrency, provider-order commits for successful out-of-order completions, serial prior-success commit before later serial error, and process-IO execution remaining serial by default. They do not cover concurrent partial failure semantics. Reasonix `internal/agent/agent.go` executes each call into a per-call `toolOutcome` and emits `ToolResult` in call order; its guards tests prove read-only parallelism while keeping writers and state-like read-only tools serial. Codex `codex-rs/core/src/tools/parallel.rs` converts non-fatal `FunctionCallError` into the failing call's output while keeping fatal join/runtime errors distinct.
- 验证: Add focused tests around `executeToolBatchesWithRunner` and at least one real `resource_read` path. The tests should fail if a model-actionable per-call read failure suppresses sibling successful results without a deliberate fatal reason, or if a fatal runner error is silently presented as a normal read result. Re-run `go test ./internal/kernel -run "TestExecuteToolBatches|TestPlanToolExecutionBatches|TestToolScheduling" -count=1`, then `go test ./internal/kernel -count=1` before retirement.
- 优先级: P2.
- Gap: This is a contract and test gap for the newly enabled concurrent execution path. The current implementation is conservative for `resource_read`, but the failure taxonomy must be locked before additional pure-read/resource primitives make parallel batches common production behavior.
- Next slice: Extend the Tool Scheduling requirement/design with per-call versus fatal parallel failure semantics, then add the regression tests before changing the runner. Do not solve this by exposing scheduling metadata to the model or by letting provider-native `parallel_tool_calls` decide failure behavior.
- Reference alignment: Aligned with Reasonix's per-call outcome model and Codex's per-call failure response for non-fatal tool errors while preserving Genesis's stricter effect-class scheduler. The active drift risk is collapsing all parallel-batch failures into a single `turn.failed` or, in the opposite direction, treating executor/runtime failures as ordinary model-repairable read results.

### KERNEL-UI-TIMELINE-DETAIL-INFORMATION-DENSITY-20260625 - P2 - Timeline detail projection must expose sanitized operation diagnostics beyond the compact card

- Status: open.
- Area: Interface Kernel / UI Timeline Projection / Inspection.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`.
- 标题: Timeline detail projection must expose sanitized operation diagnostics beyond the compact card.
- 问题: Genesis now correctly projects turns as a tree, keeps running work open, collapses settled processing groups, and separates raw event/audit/context projections. However, `UITimelineDetail` currently returns the selected `UITimelineItem` from the already compact timeline, and `operation_detail` does not expose a sanitized command preview, per-operation duration, or a distinct bounded detail output beyond the same preview fields shown on the compact card. When a user expands a tool/command card, the shell still lacks enough controlled information to understand what ran and what happened without falling back to raw kernel events or audit JSON.
- 建议: Extend the protected timeline detail projection, not the main chat timeline, with sanitized operation diagnostics: command or primary-argument preview derived from the registered tool call shape, operation/job duration, bounded visible output with truncation metadata, and an optional detail ref for deeper protected inspection. Keep raw event IDs, provider tool-call IDs, operation IDs, sandbox leases, credential refs, raw payload JSON, and unredacted output out of UI detail. Add tests proving the main settled timeline stays compact while detail for a failed shell/tool operation includes the safe command preview, duration, bounded redacted output, and truncation flags.
- 证据: Genesis `internal/kernel/ui_timeline_projection.go` implements `UITimelineDetail` by calling `UITimeline` and returning the selected node; `inspection_types.go` exposes `DurationMs` and output preview fields but no command preview or visible detail-output field; current `timeline_projection_test.go` verifies detail selection and redaction but names the behavior "ReturnsSelectedNodeWithoutDiagnostics". `docs/design/kernel-foundation-capabilities.md` already defines `OperationDetailProjection` with `command_preview`, `status`, `duration`, `visible_output`, `truncation`, and `detail_ref?`, and states that `operation_detail` should expose command preview, status, duration, bounded visible output, and truncation metadata. Reasonix `internal/agent/agent.go` emits `ToolDispatch` before execution and `ToolResult` after execution with output, truncation, and `DurationMs`; Reasonix `internal/cli/toolcard.go` and `toolcard_test.go` prove tool cards render readable command/argument previews instead of raw JSON such as `job_ids` or `timeout_seconds`.
- 验证: Add focused tests for at least a failed shell command, a truncated `resource_read`/read operation, and a job-backed operation. The tests should assert that `/sessions/{id}/timeline` remains compact and redacted; `/sessions/{id}/timeline/details/{detail_ref}` exposes only sanitized command or primary-argument preview, duration, bounded output, source/truncation metadata, and no raw event IDs, provider IDs, operation IDs, hidden control fields, credentials, or raw payload JSON. Re-run `go test ./internal/kernel -run "TestUITimeline|TestHTTP.*Timeline|Test.*Timeline.*Detail" -count=1`, then `go test ./internal/kernel -count=1` before retirement.
- 优先级: P2.
- Gap: This is a current-stage projection and behavior-test gap, not a request for a full GUI. The compact timeline and raw audit projection already exist; the missing middle layer is the user-facing expandable detail projection needed to inspect long-running turns without polluting the main chat stream or exposing raw kernel facts.
- Next slice: Update the foundation capability design if necessary to name the exact detail fields, then implement the timeline-detail projection as a separate sanitized read model. Do not solve this in the WebUI by interpreting raw `KernelEventLog`, and do not add tool-specific UI code that bypasses the Interface Kernel projection owner.
- Reference alignment: Aligned with Reasonix's split between chronological tool dispatch/result facts and readable tool cards that show concise argument previews, duration, output, and truncation. Genesis intentionally keeps a kernel-owned tree projection instead of Reasonix's CLI-local rendering, but it should not regress into a compact card with no diagnostic detail or a UI that must parse raw events.

### KERNEL-CONTEXT-RESOURCE-HYDRATION-20260625 - P2 - Full skill and long-context hydration must use generic resource/context handles

- Status: open.
- Area: Model Gateway / Resource Owner / Skill Catalog.
- Requirement: `docs/requirements/kernel-resource-read.md`.
- Design: `docs/design/kernel-resource-read.md`.
- Gap: Genesis currently has a path-free metadata-only skill index and a bounded `resource_read` primitive, but it does not yet have a production path for admitting full skill bodies, connector attachment text, or long application instructions into provider context as generic hydrated context. The current absence is intentional; the remaining gap is a generic resource/context owner flow that can admit bounded content, record derivation evidence, and let the Model Gateway render typed context fragments without reintroducing `skill.read` or caller-built prompt splicing.
- Next slice: Define and implement a generic context-hydration admission path only after resource/context ownership is selected. The implementation must keep full skill bodies out of default context, capabilities, timeline, and context inspection unless a bounded generic hydrated context handle was admitted for the turn/task.
- Evidence: `docs/requirements/kernel-resource-read.md` now defines context hydration semantics and forbids package-specific skill-body tools. `docs/design/kernel-resource-read.md` documents the current boundary: metadata-only skill index plus `resource_read`, with future hydration going through resource/context owner facts. Current tests such as `TestKernelInjectsBudgetedSkillIndexWithoutSkillBodies`, `TestSubmitTurnProjectsRegisteredToolManifestWithoutSkillCatalogContext`, and `TestTurnEvidenceRecordsModelInputKindsWithoutSkillPaths` prove the default provider context excludes skill bodies and paths.
- Verification: Future verification must prove full skill bodies are absent by default; a hydrated body appears only through a generic resource/context handle with typed model input evidence, bounded output, derivation refs, and no filesystem path or package root leakage; `skill.read`, `read_skill`, and skill-specific kernel tools remain absent from the model-visible tool manifest.
- Reference alignment: Aligned with Codex's split between bounded model-visible context fragments and skill instruction injection when selected, while intentionally avoiding a Genesis package-specific skill-read tool. Aligned with Reasonix's explicit `@resource`/MCP resource reads as on-demand context rather than always-on prompt content. The active drift risk is treating skill packages as kernel APIs or letting shells/connectors assemble provider context directly.

### KERNEL-MEMORY-MODEL-CONTEXT-SENSITIVITY-20260625 - P2 - Approved memory recall needs a model-visible sensitivity contract

- Status: open.
- Area: Accumulation / Model Gateway / Provider Context Projection / Redaction Governance.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`.
- 标题: Approved memory recall needs a model-visible sensitivity contract.
- 问题: Genesis allows semantic text fields to contain secret-shaped quoted content, which is correct for owner truth and review records. However, approved memory recall is not just a read model: `approvedMemoryContext` renders approved `MemoryRecall.Text` directly into `Approved memories:` provider context on future turns. There is no separate sensitivity decision or redaction guard for the transition from approved memory truth to model-visible long-lived context, so an accidentally approved API key, token, password, or copied credential can be replayed to providers across sessions.
- 建议: Define the approved-memory model-context boundary explicitly. The conservative P0 contract should keep raw memory truth in the Accumulation owner but project a bounded/redacted model-visible memory context unless an explicit future sensitivity/grant policy says otherwise. Tests should distinguish semantic storage from provider projection: secret-shaped text may remain in owner truth and review evidence, while provider context, provider_command JSON, context inspection, session projection, and UI timeline receive only the approved model-visible projection.
- 证据: `internal/kernel/model_context.go::approvedMemoryContext` appends `MemoryRecall.Text` without redaction. `internal/kernel/memory.go::recallMemories` copies approved candidate text directly into `MemoryRecall.Text`. `TestKernelBuildsApprovedMemoryContextBeforeOpenAICompatibleProvider` proves approved memories are sent to the OpenAI-compatible provider request. Existing projection tests redact recalled memories in `ContextInspection` and `Session`, and `TestSemanticTextFieldsAllowSecretShapedContent` proves semantic fields are allowed to preserve secret-shaped examples, but there is no test proving approved memory provider context is redacted or sensitivity-gated. The foundation requirement says raw secrets must not appear in context, logs, readiness, events, or model-visible results.
- 验证: Add a focused test that approves a memory containing `GENESIS_PROVIDER_API_KEY=sk-memory-secret` or `Authorization: Bearer ...`, triggers a later recall, and asserts the provider request / `ProviderContextProjection` / provider_command request do not contain the raw secret while still containing a useful redacted memory projection. Also assert Accumulation owner truth is not mutated by projection redaction, and that context/session/timeline projections remain redacted.
- 优先级: P2.
- Gap: This is a current-stage guard gap because memory approval and provider-context recall already exist. It should not be solved by rejecting all secret-shaped semantic text at candidate creation, because the project already decided that semantic fields may quote such content; the missing layer is the owner-approved model-visible projection.
- Next slice: Update the memory/Model Gateway requirement/design to name the memory-context sensitivity policy, then implement the projection at the `approvedMemoryContext` or shared model-visible-context fragment layer. Avoid prompt-only instructions telling the model not to store secrets; this must be enforced by owner validators/projections and tests.
- Reference alignment: Aligned with Codex's separation between model-visible context snapshots and redacted inspection snapshots, and with Reasonix's memory-as-context behavior as a warning that memory indexes can become automatic prompt input. Genesis differs because it has explicit approval and owner truth, so it should exploit that owner boundary to create a safe provider projection rather than treating approved memory text and provider-visible memory text as identical.

### KERNEL-RESOURCE-READ-MODEL-RESULT-REDACTION-20260625 - P2 - resource_read model-visible results must apply the same redaction boundary as other tool evidence

- Status: open.
- Area: Resource Owner / Tool Runtime / Model Gateway / Projection Governance.
- Requirement: `docs/requirements/kernel-resource-read.md`.
- Design: `docs/design/kernel-resource-read.md`.
- 标题: resource_read model-visible results must apply the same redaction boundary as other tool evidence.
- 问题: `resource_read` returns bounded content, but the model-visible `tool.result` appears to carry the raw resource text. UI timeline detail has a redaction test, and historical conversation text redacts stored tool-result content when rendered as conversation history, but the live same-turn tool result that is sent to the next provider step is built from `ModelToolResult.Content` directly. This leaves a gap where credential-shaped text inside an admitted resource can become model-visible before UI/history redaction policies apply.
- 建议: Treat `resource_read` output as a model-visible projection, not as raw resource truth. Keep raw body ownership in the resource/object owner, but return a bounded redacted projection in `ModelResourceReadResult.Text` and record enough metadata/ref evidence for audit. Add tests proving raw resource text can contain secret-shaped material while the provider-visible `tool.result`, provider_command request JSON, OpenAI-compatible tool message, UI timeline/detail, and later same-session history all see only the redacted projection. If a future policy intentionally allows secret-bearing resources to be model-visible, require an explicit sensitivity/grant decision before relaxing the default.
- 证据: `internal/kernel/resource_registry.go` reads `item.text` into `ModelResourceReadResult.Text`; `internal/kernel/model_tools.go` marshals that result directly into `ModelToolResult.Content`; `internal/kernel/kernel.go` rebuilds `ModelRequest.ToolRounds` from stored `tool.result` content; `internal/kernel/provider_command.go` forwards tool-round result content to provider_command unchanged. Current `TestUITimelineResourceReadResultUsesTextPreview` proves UI redaction only, not provider-visible redaction. Codex reference tests `codex-rs/core/tests/suite/tools.rs` and `codex-rs/core/tests/suite/unified_exec.rs` prove denied/sensitive read output must not leak into function-call output sent back to the model. Reasonix `internal/doctor/report_test.go` proves diagnostic projections strip API keys, Authorization headers, and proxy secrets while preserving useful host evidence.
- 验证: Add a focused kernel test where `resource_read` reads text containing `GENESIS_PROVIDER_API_KEY=sk-resource-secret`, then assert the second provider request's `ToolRounds[0].Results[0].Content` does not contain the secret and does contain a redaction marker. Also assert provider_command/OpenAI-compatible request serialization, UI timeline/detail, and subsequent same-session provider history stay redacted while raw resource owner storage is not mutated or replaced by the projection.
- 优先级: P2.
- Gap: Genesis has a generic `resource_read` primitive and UI redaction, but it lacks an executable guard for model-visible resource-result redaction. This is a current-stage guard gap because `resource_read` is already a model-visible tool and can already feed the next provider step.
- Next slice: Add the redacted model-visible projection at the `resource_read` result boundary or a shared model-visible tool-result projection layer, then back it with provider-request, provider-command, timeline/detail, and conversation-history regression tests.
- Reference alignment: Aligned with Codex's source/sandbox tests that prevent denied sensitive file content from reaching tool output, and with Reasonix's redacted diagnostic projection pattern. Genesis's intentional difference is that admitted resource bodies may be raw in the resource owner, but model-visible tool results should be bounded/redacted projections unless a future sensitivity/grant policy explicitly says otherwise.

### KERNEL-SANDBOX-APPROVAL-NEXT-20260623 - P2 - Stronger sandbox and approval policy beyond the minimal profile split

- Status: open.
- Area: Authority Plane / Tool Runtime.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`.
- Gap: The current foundation separates `permission_mode`, `authority_policy`, `sandbox_profile`, and `approval_policy`; it now fails closed when a configured stronger sandbox profile is unavailable and blocks write tools with structured `approval_required` feedback when `approval_policy=on_request`. Requirement/design now define the future production flow for kernel-owned approval requests/decisions and sandbox enforcement evidence. Remaining gap is real OS-level workspace sandbox enforcement and an interactive approval owner implementation that can approve or deny requests without shell/UI-local authority logic.
- Next slice: Implement an actual sandbox enforcement adapter or an approval owner command path only through kernel-owned profile resolution and typed control-plane events. Do not let tool arguments, shell transports, or UI state select permission mode, sandbox profile, approval policy, or approval outcome.
- Evidence: Current docs and tests state `controlled_workspace` is not an OS sandbox and provider-visible tool results must not include permission/profile control-plane fields. `docs/requirements/kernel-foundation-capabilities.md` and `docs/design/kernel-foundation-capabilities.md` now specify that future approval approval/denial must be kernel-owned typed control-plane evidence before execution, and that unavailable stronger sandbox profiles remain blockers rather than host fallback. `TestSubmitTurnBlocksUnavailableSandboxProfileBeforeExecution` proves `os_workspace` fails closed without file effects when unavailable. `TestSubmitTurnBlocksReadOnlySandboxOverrideBeforeExecution` proves a configured `read_only` sandbox profile cannot be recorded while default/yolo silently execute through host shell. `TestSubmitTurnBlocksApprovalRequiredBeforeExecution` proves `on_request` write tools return structured approval feedback without execution. `TestSubmitTurnPlanOnRequestKeepsReadOnlyDenialBeforeApproval` proves plan mode remains a hard read-only denial rather than becoming an approval path. `TestArchitectureBoundarySandboxProfileCannotBroadenPermissionMode` proves profile overrides cannot broaden or misrepresent executable profiles. `TestArchitectureBoundaryApprovalOnRequestBlocksWriteToolsAtAdmission` proves approval gating happens at admission while read tools remain allowed. `TestSubmitTurnReturnsRepairFeedbackForUnknownModelToolArgumentFields` proves model-supplied `permission_mode`, `authority_policy`, `sandbox_profile`, and `approval_policy` fields are repairable invalid requests with no effect.
- Verification: Existing positive contracts remain true; future verification must prove real sandbox denial cannot silently degrade to host execution, approval denial returns structured feedback without execution, approval approval creates typed kernel evidence before execution, and model-supplied control-plane fields remain rejected as repairable invalid requests.
- Reference alignment: Aligned with Codex's sandbox/approval split, where approval policy is turn/control-plane state and sandbox permissions are execution-layer enforcement, and with Reasonix's separation of permission gate, interactive approval, and sandbox wrapper. The active drift risk is over-promising `default` as a real OS sandbox or turning approval into shell/UI-local logic.

### KERNEL-SHELL-ENVIRONMENT-POLICY-20260625 - P2 - shell_exec host execution needs a kernel-owned environment policy

- Status: open.
- Area: Tool Runtime / Shell Execution / Authority And Credential Plane.
- Requirement: `docs/requirements/kernel-foundation-capabilities.md`; `docs/requirements/kernel-shell-and-job-control.md`.
- Design: `docs/design/kernel-foundation-capabilities.md`; `docs/design/kernel-shell-and-job-control.md`.
- 标题: shell_exec host execution needs a kernel-owned environment policy.
- 问题: Foreground `shell_exec` and local managed jobs currently run host shell processes without assigning `cmd.Env`, so they inherit the Genesis daemon process environment. That is acceptable only if it is an explicit kernel policy; today it is an implicit Go default. Provider commands already use an explicit environment boundary that rejects secret-shaped entries, but shell processes do not. This leaves a production gap where provider keys, connector tokens, profile paths, or other daemon-local secrets can become visible to model-controlled commands such as `env`, `set`, or PowerShell environment reads whenever host/yolo execution is enabled.
- 建议: Add a shell environment policy separate from sandbox and approval. The policy should construct the environment passed to foreground shell and managed-job processes, with a small required baseline such as `PATH`/platform execution basics, explicit allowlist/denylist behavior, and fail-closed or redacted handling for credential-shaped names and values. Default controlled-workspace commands can stay environment-free, but any real host shell path must receive a kernel-owned env projection rather than daemon inheritance by omission.
- 证据: `internal/kernel/process_runtime.go::runShellProcessContextWithOutput` creates `platformShellCommand`, sets `cmd.Dir`, stdout/stderr, process termination, and timeout behavior, but never sets `cmd.Env`. `internal/kernel/managed_job_executor.go` calls the same shell process helper for background jobs. In contrast, `internal/kernel/provider_command.go` sets `cmd.Env = providerCommandEnvironment(p.env)` and validates provider-command env entries. Local Codex constructs shell `ExecParams.env` from `shell_environment_policy` in `codex-rs/core/src/tools/handlers/shell/shell_command.rs`, with tests comparing the resulting env. Local Reasonix centralizes foreground/background bash env through `bashCommandEnv(ctx)` instead of leaving each command path implicit.
- 验证: Add focused tests that set secret-shaped host env variables before invoking host/yolo shell and assert the command cannot observe them unless an explicit future grant says so. Test both foreground shell and managed job execution, and assert provider_command env behavior remains unchanged. Also add a positive test for required execution variables such as `PATH` or platform shell lookup so the policy does not break ordinary commands.
- 优先级: P2.
- Gap: This is not the same as the stronger OS sandbox issue. A sandbox can restrict filesystem/network while still leaking process environment, and yolo/host mode still needs a deliberate credential boundary. The current implementation may be acceptable for a lab spike, but it should not become the production default by accident.
- Next slice: Specify shell env policy fields in the shell/job-control requirement before implementation. Do not put env selection in model-visible tool arguments. Do not solve this by filtering UI output after the model has already run `env`; the tool process must not receive unintended secrets in the first place.
- Reference alignment: Aligned with Codex's `shell_environment_policy` and explicit `ExecParams.env` contract. Reasonix is less strict about inherited env but still centralizes command env construction in one helper. Genesis should follow the kernel/credential-plane boundary already used by `provider_command` and avoid implicit daemon-env inheritance for model-controlled shell execution.

### KERNEL-TEST-SURFACE-OWNER-SPLIT-20260625 - P3 - Kernel behavior tests still centralize multiple owner surfaces in one file

- Status: open.
- Area: Architecture Governance / Test Governance.
- Requirement: `docs/requirements/kernel-owner-structure-governance.md`.
- Design: `docs/design/kernel-owner-structure-governance.md`.
- Gap: Production code has started moving owner replay, DTOs, HTTP transport, timeline, resource, job, and tool scheduling behavior into named files, but the main behavior test surface still keeps many unrelated owner contracts in `internal/kernel/kernel_test.go`. This is not a line-count issue; the risk is that future changes to memory, work, provider, shell, job, tool-loop, HTTP, and projection contracts keep landing in one central test file, making owner drift harder to review and making it easier to treat `Kernel` as the only testable authority surface.
- Next slice: When an owner/topic is next touched, move the affected tests and local helpers into owner/topic files such as memory, work, shell/job, provider gateway, tool loop, HTTP transport, projection, or readiness tests. Do this incrementally with no runtime behavior change, no compatibility helpers, and no arbitrary file-size threshold. Keep cross-owner pressure scenarios in dedicated pressure tests instead of the central kernel behavior file.
- Evidence: `internal/kernel/kernel_test.go` currently contains 153 tests and about 347 KB of mixed contracts, including submit-turn, HTTP, shell execution, WorkRegistry, memory review/recall/supersede, provider command, OpenAI-compatible tool loop, permission/sandbox/approval, managed jobs, job control, truncation, and repair feedback. Local Codex reference tests are large but organized as topic suites such as `compact.rs`, `approvals.rs`, `unified_exec.rs`, and `realtime_conversation.rs`; local Reasonix tests are similarly owner/topic oriented under `internal/agent`, `internal/tool`, `internal/provider`, `internal/acp`, and UI packages.
- Verification: Future verification should prove moved tests still pass with `go test ./internal/kernel -count=1` and `go test ./... -count=1`, and a governance review should confirm no new owner/topic cluster was added to `kernel_test.go` when a more specific test file exists. This issue must not introduce permanent tests that only assert line counts or subjective organization.
- Reference alignment: Aligned with Codex and Reasonix using owner/topic-oriented test suites while rejecting arbitrary size caps. The active drift risk is letting the test suite preserve the same central-owner shape that production code is trying to retire.
